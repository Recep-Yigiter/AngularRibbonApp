import { Component, EventEmitter, Injectable, Input, OnInit, Output } from '@angular/core';

@Injectable({
  providedIn: 'root',
})

@Component({
  selector: 'dialog-action',
  templateUrl: './dialog-action.html',
  styleUrls: ['./dialog-action.scss']
})

export class DialogAction {

}